maven -version

-----individual phases-----
clean : delete target directory
validate : validate if project is correct
compile : compile source code ,clases stored in target
test : run tests
package : take the compile code and package it in its distributable format (JAR,WAR etc)verify : run any checks to verify the package is valid and meets quality critero=ia
install : install the package into the local repository
deploy : copies the final package to the remote repository